{
  "name": "@alia/protocol",
  "version": "0.1.0",
  "private": true,
  "description": "Shared v0.1 event protocol for Project Alia Rebuild.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "lint": "pnpm exec tsc -p tsconfig.json --noEmit",
    "typecheck": "pnpm exec tsc -p tsconfig.json --noEmit",
    "test": "node --experimental-transform-types --test test/*.test.mjs"
  }
}
